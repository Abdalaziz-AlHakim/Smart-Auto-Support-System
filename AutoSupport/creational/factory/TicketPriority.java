package creational.factory;

/**
 * Enumeration representing the priority level of a support ticket.
 */
public enum TicketPriority {
    /** Standard priority — will be handled in normal order. */
    NORMAL,
    /** High priority — requires immediate attention. */
    URGENT
}
